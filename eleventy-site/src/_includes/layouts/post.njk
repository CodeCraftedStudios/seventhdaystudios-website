---
layout: base.njk
extraStyles: |
    .blog-container {
        display: grid;
        grid-template-columns: 1fr 320px;
        gap: 3rem;
        max-width: 1200px;
        margin: 0 auto;
        padding: 8rem 2rem 5rem;
    }

    .blog-post {
        background: linear-gradient(135deg, rgba(45, 27, 78, 0.4), rgba(26, 15, 46, 0.6));
        border: 1px solid rgba(212, 168, 75, 0.15);
        border-radius: 16px;
        overflow: hidden;
    }

    .post-header-image {
        position: relative;
        aspect-ratio: 21/9;
        background: linear-gradient(135deg, var(--royal-purple), var(--mystic-purple));
        overflow: hidden;
    }

    .post-header-image::before {
        content: '';
        position: absolute;
        inset: 0;
        background: 
            linear-gradient(135deg, rgba(212, 168, 75, 0.1) 0%, transparent 50%),
            linear-gradient(225deg, rgba(74, 44, 122, 0.5) 0%, transparent 50%);
        z-index: 1;
    }

    .post-header-image::after {
        content: '';
        position: absolute;
        inset: 0;
        background-image: 
            linear-gradient(rgba(212, 168, 75, 0.03) 1px, transparent 1px),
            linear-gradient(90deg, rgba(212, 168, 75, 0.03) 1px, transparent 1px);
        background-size: 25px 25px;
    }

    .post-header-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .post-header-inner {
        position: absolute;
        inset: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        z-index: 2;
    }

    .post-header-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        filter: drop-shadow(0 4px 20px rgba(0, 0, 0, 0.4));
    }

    .post-header-label {
        font-family: 'Cinzel', serif;
        font-size: 1rem;
        color: var(--divine-gold);
        letter-spacing: 3px;
        text-transform: uppercase;
    }

    .post-category {
        position: absolute;
        top: 1.5rem;
        left: 1.5rem;
        padding: 0.4rem 1rem;
        background: rgba(26, 15, 46, 0.9);
        border: 1px solid rgba(212, 168, 75, 0.3);
        border-radius: 6px;
        font-size: 0.75rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        color: var(--divine-gold);
        z-index: 3;
    }

    .post-content {
        padding: 2.5rem;
    }

    .post-meta {
        display: flex;
        align-items: center;
        gap: 1.5rem;
        margin-bottom: 1.5rem;
        flex-wrap: wrap;
    }

    .post-date {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.9rem;
        color: var(--text-muted);
    }

    .post-date svg {
        width: 16px;
        height: 16px;
        stroke: var(--divine-gold);
    }

    .post-author {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-left: auto;
    }

    .author-avatar {
        width: 36px;
        height: 36px;
        background: linear-gradient(135deg, var(--divine-gold), var(--light-gold));
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1rem;
    }

    .author-info {
        display: flex;
        flex-direction: column;
    }

    .author-name {
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--text-light);
    }

    .author-role {
        font-size: 0.75rem;
        color: var(--text-muted);
    }

    .post-title {
        font-family: 'Cinzel', serif;
        font-size: 2.2rem;
        color: var(--pale-gold);
        margin-bottom: 1.5rem;
        line-height: 1.3;
    }

    .post-body h2 {
        font-family: 'Cinzel', serif;
        font-size: 1.5rem;
        color: var(--divine-gold);
        margin: 2.5rem 0 1rem;
        position: relative;
        padding-left: 1rem;
    }

    .post-body h2::before {
        content: '';
        position: absolute;
        left: 0;
        top: 0;
        bottom: 0;
        width: 3px;
        background: linear-gradient(180deg, var(--divine-gold), var(--mystic-purple));
        border-radius: 2px;
    }

    .post-body h3 {
        font-family: 'Cinzel', serif;
        font-size: 1.2rem;
        color: var(--light-gold);
        margin: 2rem 0 0.75rem;
    }

    .post-body p {
        font-size: 1.05rem;
        color: var(--text-light);
        line-height: 1.85;
        margin-bottom: 1.25rem;
    }

    .post-body ul, .post-body ol {
        margin: 1.25rem 0;
        padding-left: 1.5rem;
    }

    .post-body li {
        font-size: 1.05rem;
        color: var(--text-light);
        line-height: 1.8;
        margin-bottom: 0.75rem;
    }

    .post-body ul li::marker {
        color: var(--divine-gold);
    }

    .post-body ol li::marker {
        color: var(--divine-gold);
        font-weight: 600;
    }

    .post-body strong {
        color: var(--light-gold);
        font-weight: 600;
    }

    .post-body em {
        color: var(--text-muted);
        font-style: italic;
    }

    .post-body a {
        color: var(--divine-gold);
        text-decoration: underline;
        text-underline-offset: 2px;
    }

    .post-body a:hover {
        color: var(--light-gold);
    }

    .post-body blockquote {
        margin: 2rem 0;
        padding: 1.5rem 2rem;
        background: linear-gradient(135deg, rgba(212, 168, 75, 0.1), rgba(212, 168, 75, 0.05));
        border-left: 4px solid var(--divine-gold);
        border-radius: 0 12px 12px 0;
        font-style: italic;
        color: var(--text-light);
    }

    .post-footer {
        margin-top: 3rem;
        padding-top: 2rem;
        border-top: 1px solid rgba(212, 168, 75, 0.1);
    }

    .post-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        margin-bottom: 2rem;
    }

    .post-tag {
        padding: 0.4rem 1rem;
        background: rgba(74, 44, 122, 0.4);
        border: 1px solid rgba(212, 168, 75, 0.15);
        border-radius: 20px;
        font-size: 0.8rem;
        color: var(--text-muted);
        text-decoration: none;
        transition: all 0.3s ease;
    }

    .post-tag:hover {
        background: rgba(212, 168, 75, 0.1);
        border-color: rgba(212, 168, 75, 0.3);
        color: var(--divine-gold);
    }

    .back-to-blog {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--divine-gold);
        text-decoration: none;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        font-size: 0.9rem;
        transition: all 0.3s ease;
    }

    .back-to-blog:hover {
        color: var(--light-gold);
    }

    .back-to-blog svg {
        width: 16px;
        height: 16px;
        transition: transform 0.3s ease;
    }

    .back-to-blog:hover svg {
        transform: translateX(-4px);
    }

    /* Sidebar */
    .blog-sidebar {
        display: flex;
        flex-direction: column;
        gap: 2rem;
    }

    .sidebar-widget {
        background: linear-gradient(135deg, rgba(45, 27, 78, 0.4), rgba(26, 15, 46, 0.6));
        border: 1px solid rgba(212, 168, 75, 0.15);
        border-radius: 12px;
        padding: 1.5rem;
    }

    .widget-title {
        font-family: 'Cinzel', serif;
        font-size: 1rem;
        color: var(--divine-gold);
        margin-bottom: 1rem;
        padding-bottom: 0.75rem;
        border-bottom: 1px solid rgba(212, 168, 75, 0.15);
    }

    .about-widget-content {
        text-align: center;
    }

    .about-avatar {
        width: 80px;
        height: 80px;
        margin: 0 auto 1rem;
        background: linear-gradient(135deg, var(--divine-gold), var(--light-gold));
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
    }

    .about-name {
        font-family: 'Cinzel', serif;
        font-size: 1.1rem;
        color: var(--pale-gold);
        margin-bottom: 0.25rem;
    }

    .about-role {
        font-size: 0.85rem;
        color: var(--divine-gold);
        margin-bottom: 1rem;
    }

    .about-bio {
        font-size: 0.9rem;
        color: var(--text-muted);
        line-height: 1.7;
    }

    .status-item {
        display: flex;
        justify-content: space-between;
        padding: 0.75rem 0;
        border-bottom: 1px solid rgba(212, 168, 75, 0.1);
    }

    .status-item:last-child {
        border-bottom: none;
    }

    .status-label {
        font-size: 0.85rem;
        color: var(--text-muted);
    }

    .status-value {
        font-size: 0.85rem;
        color: var(--text-light);
        font-weight: 500;
    }

    .status-value.active {
        color: var(--divine-gold);
    }

    @media (max-width: 1024px) {
        .blog-container {
            grid-template-columns: 1fr;
            padding: 6rem 1rem 4rem;
        }

        .blog-sidebar {
            order: 2;
        }
    }

    @media (max-width: 600px) {
        .post-content {
            padding: 1.5rem;
        }

        .post-title {
            font-size: 1.75rem;
        }

        .post-meta {
            flex-direction: column;
            align-items: flex-start;
            gap: 1rem;
        }

        .post-author {
            margin-left: 0;
        }
    }
---

<main class="blog-container">
    <article class="blog-post">
        <div class="post-header-image">
            {% if thumbnail %}
                <img src="{{ thumbnail }}" alt="{{ title }}">
            {% else %}
                <div class="post-header-inner">
                    <span class="post-header-icon">📖</span>
                    <span class="post-header-label">Dev Blog</span>
                </div>
            {% endif %}
            {% if category %}
                <span class="post-category">{{ category }}</span>
            {% endif %}
        </div>

        <div class="post-content">
            <div class="post-meta">
                <div class="post-date">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                        <line x1="16" y1="2" x2="16" y2="6"></line>
                        <line x1="8" y1="2" x2="8" y2="6"></line>
                        <line x1="3" y1="10" x2="21" y2="10"></line>
                    </svg>
                    {{ date | dateFormat }}
                </div>
                <div class="post-author">
                    <div class="author-avatar">✝️</div>
                    <div class="author-info">
                        <span class="author-name">{{ author | default: "Seventh Day Studios" }}</span>
                        <span class="author-role">{{ authorRole | default: "Founder" }}</span>
                    </div>
                </div>
            </div>

            <h1 class="post-title">{{ title }}</h1>

            <div class="post-body">
                {{ content | safe }}
            </div>

            <div class="post-footer">
                {% if tags %}
                <div class="post-tags">
                    {% for tag in tags %}
                        {% if tag != "post" %}
                        <span class="post-tag">{{ tag }}</span>
                        {% endif %}
                    {% endfor %}
                </div>
                {% endif %}
                
                <a href="/blog/" class="back-to-blog">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M19 12H5M12 19l-7-7 7-7"/>
                    </svg>
                    Back to Blog
                </a>
            </div>
        </div>
    </article>

    <aside class="blog-sidebar">
        <div class="sidebar-widget">
            <h3 class="widget-title">About the Developer</h3>
            <div class="about-widget-content">
                <div class="about-avatar">✝️</div>
                <h4 class="about-name">Seventh Day Studios</h4>
                <p class="about-role">Founder & Developer</p>
                <p class="about-bio">
                    A faith-driven game development studio on a mission to create high-quality 
                    Christian games that glorify God and engage players worldwide.
                </p>
            </div>
        </div>

        <div class="sidebar-widget">
            <h3 class="widget-title">Project Status</h3>
            <div class="status-item">
                <span class="status-label">Current Project</span>
                <span class="status-value">The Scripture Journey</span>
            </div>
            <div class="status-item">
                <span class="status-label">Status</span>
                <span class="status-value active">In Development</span>
            </div>
            <div class="status-item">
                <span class="status-label">Platform</span>
                <span class="status-value">Roblox</span>
            </div>
        </div>
    </aside>
</main>
